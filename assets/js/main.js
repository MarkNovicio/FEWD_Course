function increaseTitleSize(){
    let element = document.querySelector(".fewd-content");
    let title = document.querySelector(".content-title")
    element.onmouseover=function(){
        title.css
    }
}


function increaseTitleSize() {
	window.addEventListener(
	  "click",
	  function (event) {
		  
		if (event.target.classList.contains("fewd-content")) {
			console.log("worked")
			//console.log(document.getElementsByClassName("signin-messages-container"))
            
		 // document.getElementsByClassName("signin-messages-container")[0].style.display = "none"  //getElementsByClassName("signin-messages-container")  
		  //delivers a nodeList.
		}
		  this.console.log("selectedElement works!!")})}

selectedElement()